from enum import Enum, auto


class PotionType(Enum):
    HEALING = auto()
    VISION = auto()


class PillarType(Enum):
    ABSTRACTION = auto()
    ENCAPSULATION = auto()
    INHERITANCE = auto()
    POLYMORPHISM = auto()
